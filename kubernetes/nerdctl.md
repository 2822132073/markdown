# 安装

```bash
export version=1.1.0
wget https://github.com/containerd/nerdctl/releases/download/v${version}/nerdctl-full-${version}-linux-amd64.tar.gz
tar xf /usr/local nerdctl-full-${version}-linux-amd64.tar.gz
```